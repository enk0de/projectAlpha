# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'projectAlpha' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'FloatingPanel'
  pod 'SwiftyBeaver'

  # Pods for projectAlpha

  target 'projectAlphaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'projectAlphaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
